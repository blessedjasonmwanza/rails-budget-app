<%= form_with(model: category, class:'new_category') do |form| %>
  <% if category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
        <% category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class='input-fields'>
      <%= form.text_field :name, autofocus: true, placeholder: "Name", class: "form-control", required: true %>
    
      <%= form.file_field :icon, placeholder: "Image", class: "form-control", required: true %>
  </div>
  <div class="actions">
    <%= form.submit  class: "btn" %>
  </div>
<% end %>
